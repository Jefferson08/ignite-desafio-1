import logo from "../assets/logo.svg";

export const Header = () => (
    <header className="bg-gray-700 lg:py-20">
        <div className="container flex justify-center">
            <img src={logo} alt=""/>
        </div>
    </header>
)
